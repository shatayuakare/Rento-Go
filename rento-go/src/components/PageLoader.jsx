import React from 'react'

const PageLoader = () => {
    return (
        <section className="flex items-center bg-white justify-center bg-transparent backdrop-blur">
            <span className="loading loading-ring loading-lg"></span>
        </section>
    )
}

export default PageLoader